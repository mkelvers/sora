# PostgreSQL connection string.
DATABASE_URL=postgres://localhost:5432/unique
# At least 32 random characters; signs stream proxy tokens. Generate with: openssl rand -base64 48
STREAM_SIGNING_SECRET=
