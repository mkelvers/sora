-- The watchlist enum and table were created by migration 0008 and upgraded by
-- migration 0013. Their application schema was later removed without a
-- database migration, so the database objects remained. This migration only
-- reconciles Drizzle's schema snapshot as the feature becomes active again.
SELECT 1;
